import React from 'react';
import Employers from '../components/Employers';

const TableEmployersContainer = (props) => {
    return (
        <div>
            <Employers {...props} />
        </div>
    );
}

export default TableEmployersContainer; 
import React from 'react';
import Aids from '../components/Aids';

const TableAidsContainer = (props) => {
    return (
        <div>
            <Aids {...props} />
        </div>
    );
}

export default TableAidsContainer; 